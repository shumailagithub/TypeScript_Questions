//-------------------------------------------------------Operators---------------------------------------------------------
//-------------------------------------------------------Task # 1----------------------------------------------------------
//create two variables and add both variables. print it into terminal.
let num1 = 20;
let num2 = 30;
let sum = num1 + num2;
console.log(`sum of 20 + 30  = ${sum}`);
//-------------------------------------------------------Task # 2----------------------------------------------------------
//create two variables and subtract both variables. print it into terminal.
const num3 = 20;
const num4 = 60;
const subtract = num4 - num3;
console.log(`subtract of 60 - 20 = ${subtract}`);
//-------------------------------------------------------Task # 3----------------------------------------------------------
//create two variables and multiply both variables. print it into terminal.
const num5 = 4;
const num6 = 5;
const multiply = num5 * num6;
console.log(`multiply 4 * 5 = ${multiply}`);
//-------------------------------------------------------Task # 4----------------------------------------------------------
//create two variables and divide first variable by second. print it into terminal.
const firstNumber = 7;
const secondNumber = 8;
const divition = firstNumber / secondNumber;
console.log(`divition of 7 / 8 = ${divition}`);
//-------------------------------------------------------Task # 5----------------------------------------------------------
//create two variables and use exponentiation. print it into terminal.
const firstNum = 2;
const secondNum = 6;
const exponentiation = firstNum ** secondNum;
console.log(`exponentiation of 2 ** 6 = ${exponentiation}`);
//-------------------------------------------------------Task # 6----------------------------------------------------------
//create two variables and use modulus. print it into terminal.
const first_Num = 20;
const second_Num = 60;
const modulus = first_Num % second_Num;
console.log(`modules of 20 % 60 = ${modulus}`);
export {};
